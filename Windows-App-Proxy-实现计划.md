# Windows App Proxy 实现计划

当前实现的完整解释见 [设计与实现](D:/app_proxy/Windows-App-Proxy-设计与实现.md)，包含架构、请求链路、网卡选择、Guard、MSIX、数据和验证边界。

## 当前方案：0.2（2026-09-18，覆盖下方 0.1 历史方案）

- 统一使用便携包内置的官方 sing-box 1.14.1 x64，附带原版 DLL、许可证和源码链接；打包校验压缩包 SHA256，配置检查及启动校验内置文件哈希。
- 移除外部端口发现/登记及用户选择已有内核的菜单、命令和运行路径。保留手动 HTTP/SOCKS5 上游、订阅解析、地区及节点选择，全部经过内置内核。
- Profile 仅保留 managed 类型和内置内核运行路径；删除 external 类型、命令和全部兼容分支。不支持的旧数据直接校验失败，不保留迁移逻辑。
- 自动探测上游网卡：读取 Windows 硬件属性、连接状态、IPv4 地址、默认路由；排除虚拟网卡，优先 Wi-Fi，再按路由及接口 metric 选择其他物理网卡。
- 候选网卡写入 `route.default_interface`；使用实际 sing-box 及全部托管入口验证代理联网，失败则尝试下一张物理网卡。最后沿用 mac 版回退 `route.auto_detect_interface=true`，在状态和日志中明确提示回退可能经过虚拟网卡。
- 自动探测在内核实际启动、重启及配置应用时执行；暂不增加常驻网络切换监视器。网络变化后重启内核即可重新探测。没有可用代理时拒绝启动应用，运行中更新失败恢复旧配置。
- 不修改系统路由、网卡优先级或防火墙，不实现 TUN；订阅下载和系统 DNS bootstrap 不宣称随内核绑定。
- 保留 Guard、应用快捷方式、多入口、通用 MSIX 启动和诊断。分身支持 Codex / Claude：共用独立 user-data、代理参数和按实例识别的 Guard，分别使用 CODEX_HOME / CLAUDE_CONFIG_DIR；不处理 Claude settings。Claude 2.2553.1.0 已验证双开、独立目录、网络进程连接代理及 Guard；MSIX 文件虚拟化由清单自动识别，使用包 LocalState 处理。登录后模型请求和 Code/Cowork 仍待验收。内核清理保留包内文件，只清除运行状态；原应用与数据目录保留。

实现入口：`windows/src/network.ts`、`native/bridge.ps1`（探测），`src/core.ts`（选择/验证/回退），`src/builtin.ts`、`scripts/setup-core.ts`（内核制品），`scripts/package.ts`（便携包）。当前使用流程和验收结果以 `windows/README.md`、`windows/TEST-RESULTS.md` 为准。

## 0.1 历史方案与来源核对（以下保留历史，不代表当前范围）

日期：2026-09-17；实施更新：2026-09-18

状态：已在 windows/ 实现 TypeScript 首版并执行受控端到端验证；真实 Claude/Codex 应用兼容性仍待验证。实际能力和验证边界以 windows/README.md、windows/TEST-RESULTS.md 为准。原 macOS 文件保持不变。

最新范围调整：首版加入 Guard 防误触；暂不做网卡绑定和设备指纹；sing-box 仅保留复用/安装、配置检查、运行及失败恢复，取消多版本目录和复杂事务设计。订阅直接参考并迁移原版下载、解析、地区分组、多选、配置生成和刷新流程，不另设计“原生 JSON 优先”路线，不重做协议内核。

## 1. 首版目标与范围

交付一个 Windows 用户态按应用代理工具，提供中文交互菜单、命令行和桌面快捷方式。首版包含完整的 sing-box 支持，并优先复用已有可用代理。

### 首版包含

- 已有 sing-box 和代理入口的发现、手动登记、连通性验证与复用。
- 复用已有 sing-box.exe；未安装时提供简单的官方下载安装入口，保留版本及完整性记录。
- 本工具托管实例的配置、启动、停止、重启、日志和状态管理。
- 手动 HTTP/SOCKS5 出口，支持上游认证。
- 按原版支持范围迁移订阅导入、国家/地区分组、节点多选、urltest 出口和手动刷新。
- 多个本地代理入口分别路由到不同出口。
- 应用登记、修改、删除，代理/直连绑定，桌面快捷方式。
- 启动前检查、已有应用实例提示、分层诊断。
- Guard 防误触：对已启用保护且支持识别的应用，发现绕过代理入口启动时终止该实例，再通过代理启动器拉起。
- 配置校验、更新失败回滚、资源归属记录与卸载。

### 后续功能

- 接管已有 sing-box 的配置及服务：独立迁移流程，先识别启动来源并备份，不在检测时自动接管。
- 对明确支持的应用实现分身，验证数据目录、单实例行为和实际出口。
- 可选内核登录自启、定时订阅刷新；Guard 自身的当前用户登录启动随 Guard 功能在首版提供。
- 图形界面、托盘。

暂不做网卡绑定、设备指纹环境、TUN/WFP、DLL 注入、任意应用强制代理或通用多开。Guard 仅操作明确登记并启用保护的目标；它和代理启动参数均不构成全流量隔离或零直连保证。

## 2. 两种代理使用模式

| 模式 | 可执行文件来源 | 配置与进程归属 | 工具行为 |
|---|---|---|---|
| 使用已有代理端口（external） | 无需由工具管理 | 用户或其他软件 | 验证、绑定、诊断，不修改配置、不启停 |
| 工具托管（managed） | 用户提供的已有程序，或工具下载 | 本工具使用独立配置创建的实例 | 管理节点、订阅、端口、配置与启停 |

已有 sing-box 正在运行，默认按 external 复用。已有 sing-box.exe 但没有适用实例，可以复用这个程序创建 managed 实例，无需重复下载。

检测到 sing-box 不意味着所有监听端口都是代理，也不意味着代理可以联网。发现阶段展示候选来源；通过明确代理请求验证后才标记可用。无法自动发现时允许手动填端口。

外部代理已运行但探测失败时进入诊断，不自动覆盖其配置或启动另一个实例替代它。

## 3. 用户操作流程

### 首次配置

1. 运行入口，检查 Windows、Node.js 24、系统 PowerShell 辅助能力、工具数据目录及配置版本。便携包附带 Node。
2. 展示发现的代理入口，也允许手动输入或选择工具托管。
3. external 路径：验证代理请求，保存入口绑定信息。
4. managed 路径：选择已有 sing-box.exe，缺失时下载安装；录入手动节点或导入订阅。
5. 给 managed profile 分配未占用的回环监听端口，选择出站节点，生成并检查配置。
6. 启动 managed 实例，验证端口、代理请求及出口探测。
7. 选择应用 EXE，设置工作目录、参数及代理绑定；未知应用默认只使用环境变量，Chromium 参数需明确适配。
8. 创建快捷方式，为兼容的目标配置 Guard 保护，展示代理绑定、保护状态及启动入口。

首次配置既可先添加代理再登记应用，也可通过独立 CLI 命令逐步完成。

### 日常启动

1. 根据稳定 ID 读取应用和 profile，检查文件是否存在及参数是否有效。
2. 检查同一 EXE 的已有实例。已启用 Guard 的兼容应用按第 6 节保护流程处理；其他应用无法排除复用旧进程时提示关闭后重试，不默认终止。
3. external：检查入口，不尝试接管进程。managed：核验已有实例归属；未运行则启动，正在启动则等待有界时间。
4. 执行有超时的入口及代理协议检查。公网出口探测单独显示，避免把公共探测站故障等同于内核故障。
5. 代理不可用则拒绝创建目标进程。验证结果不确定时给出原因，不静默改成直连。
6. 构造独立子进程环境、参数数组和工作目录，启动应用，记录 PID、启动时间及启动结果。
7. 关闭应用后内核继续为其他绑定应用提供服务；用户可显式停止本工具托管的内核。

直连模式清理子进程的代理变量；对于验证支持的适配器可使用其直连参数。此模式不承诺覆盖应用自身和系统代理设置。

## 4. 技术选型与模块

首版采用 Node.js + TypeScript + JSON，保留原 JXA 中的 JavaScript 解析和生成逻辑。使用 child_process.spawn 的参数数组、cwd 和独立环境块启动程序，不将用户输入拼接为 Shell 命令。

Windows 专属操作通过常驻 PowerShell 5.1 辅助进程执行，以 JSON 行协议交互；负责进程身份、关闭、ACL、快捷方式和任务计划程序。源码运行需要 Node.js 24，便携包附带运行时，用户不需要另外安装 PowerShell 7。

建议源码布局如下，不搭建通用插件框架：

```text
windows/
  App Proxy.cmd               # 双击入口
  src/
    cli.ts                   # 中文菜单和命令行
    service.ts               # 用例组织、状态和卸载
    store.ts                 # 配置、校验、写入锁、备份与归属清单
    proxy.ts                 # 代理协议探测
    core.ts                  # 程序准备、配置生成、托管进程生命周期
    subscription.ts          # 下载、选择保持与刷新
    legacy/                  # 迁移后的原版 JS 解析器和配置生成器
    applications.ts          # 登记、适配、子进程启动与状态
    guard.ts                 # 受保护应用检测、重启、防循环及开关
    integration.ts           # 快捷方式和登录任务
    native.ts                # 常驻 Windows 辅助进程通信
  native/                    # PowerShell 5.1 辅助脚本和隐藏启动入口
  tests/
    fixtures/                # 无真实凭据的订阅和配置样本
    helpers/                 # 受控应用、代理与测试端点
  examples/
  README.md
```

原 JXA 中可迁移的节点语义采用测试样本固定行为；文件读写、Base64、HTTP 下载等 macOS API 改为 Node API。迁移脚本可重现 JS 模块；新版 sing-box 的 block 出站替换为 reject 路由，删除网卡绑定行为。

## 5. 配置及状态模型

默认数据根目录：`%LOCALAPPDATA%\AppProxy`。开发和测试允许显式指定 workspace 内的独立数据根目录，不写入真实应用配置。

```text
AppProxy/
  manifest.json              # 版本化的应用/profile/节点引用
  bin/sing-box.exe           # 仅保存工具下载的一份程序，不建立多版本仓库
  config/                    # 运行配置、候选配置和可回滚版本
  state/                     # 进程归属、Guard 状态、资源归属清单
  logs/                      # 脱敏且有大小/保留限制的日志
  profiles/                  # 为后续分身预留的数据目录
```

- manifest 包含 schemaVersion；应用、profile、节点均使用稳定 ID，显示名不作为文件路径。
- application：id、名称、EXE、工作目录、参数数组、适配类型、proxy/direct 模式、profileId、guardEnabled。
- profile：id、名称、external/managed 类型、入口地址/端口；managed 额外关联节点选择和订阅来源。
- runtime：程序路径、来源、版本、下载地址及校验记录；复用用户程序时不将其登记为可卸载文件。
- 引用检查：仍被应用引用的 profile 不允许直接删除；节点选择和订阅更新必须保持有效引用。
- Windows 环境变量按不区分大小写处理。明确管理 HTTP_PROXY、HTTPS_PROXY、ALL_PROXY、NO_PROXY；代理模式的绕过规则显式配置，防止继承 NO_PROXY=* 导致探测与应用行为不一致。
- 保留父进程和系统持久环境，不盲目修改 HOME、USERPROFILE、APPDATA 或 LOCALAPPDATA。
- 首版将包含凭据的 manifest/config/backup 存在仅当前用户与 SYSTEM 可访问的目录中，使用 ACL 限制访问，未实现 DPAPI 加密。原始流量日志关闭；状态和事件日志不输出订阅 URL、节点密码或应用参数。

## 6. sing-box 管理设计

### 简单的程序准备与运行

- 优先使用已有程序；缺失时从官方来源安装一份 Windows 程序，记录实际版本、架构、下载 URL 及可用校验信息。
- 不做版本选择界面、多版本共存、自动升级/降级或版本管理服务。
- 读取实际版本用于诊断；用该程序的 sing-box check 验证生成配置，不兼容则报告，不自动替换用户的程序。
- 下载失败或完整性验证失败时不替换当前可用程序；临时下载及解压路径归工具所有，校验解压路径边界。

### 多出口

- 首版默认一个 managed sing-box 进程承载多个 profile，每个入口仅监听 127.0.0.1。
- 每个入口按 inbound 映射到该 profile 的出站；沿用原版节点多选及 urltest 分组，实际测速与选择交给 sing-box，单节点作为该机制的简单情况。
- 多个应用可以共享一个 profile；绑定同一入口不代表有不同出口。
- 端口占用检查只是预检查，仍须处理实际绑定时的竞争与内核退出。
- 配置格式以实际使用且经过测试的内核为准，包括 DNS、route action 和废弃字段；不照搬原脚本旧格式。暂不加入网卡探测、绑定和回退逻辑。

### 最小配置更新流程

1. 加写入锁，生成临时配置并执行 sing-box check；失败不改旧配置。
2. 将配套的 manifest 和运行配置保存为一份最近可用备份，写入新配置并重启本工具的实例。
3. 检查启动及代理入口；失败则恢复这份备份，使用同一个程序重新启动，报告恢复结果。
4. 下次运行校验配置一致性和实际状态，无法确认时报告并提供备份恢复，不搭建通用事务日志或多版本恢复框架。

进程归属至少结合 PID、创建时间、可执行文件路径和工具配置路径核验；不能只凭 PID 或进程名停止进程。并发启动使用相同的锁，避免重复内核。

### Guard 防误触（首版）

- 每应用可启用、停用及查看保护状态。首次启用时注册本工具的当前用户登录任务，运行单个后台 Guard；关闭保护和卸载时撤销对应资源。应用配置直连时不执行代理纠正。
- 周期检查当前用户、当前交互会话内已登记的精确 EXE 路径，结合创建时间、主/辅助进程关系和适配器的代理标识识别目标。不能按同名 EXE 批量终止，也不能将任意辅助进程缺少参数视为绕过代理。
- 对 Chromium/Electron 适配器核对主进程的实际代理参数、冲突参数及预期入口；工具启动记录仅辅助识别，不能替代实际参数检查。
- 仅靠环境变量代理且无法可靠读取或验证的应用，明确标记 Guard 暂不支持，不把所有未知进程当成违规实例。
- 确认受保护应用绕过启动器后，先请求正常关闭，超时后重新核验 PID/创建时间/路径，再终止该目标实例；随后经统一启动器按已保存配置拉起。
- 如果代理不可用，不重新启动目标，显示保护阻止启动的原因。提供可见日志和 Guard 暂停/停用入口。
- 使用每应用锁、启动宽限期、重启冷却和有限重试，避免 Guard 与启动器竞争、辅助进程误判及无限重启。
- 代理绑定修改后，通过工具正常启动的既有实例不会仅因配置变更被强制重启；记录其实际使用的绑定，新绑定下次启动生效，显式重启时才切换。
- 外部代理模式同样可以保护目标应用，但 Guard 不获得停止或修改外部 sing-box 的权限。
- 验收先使用受控应用；不能在开发测试中自动关闭用户当前工作的真实应用。运行中的 Guard 可纠正误启动，但检测前存在时间窗口，不承诺杜绝直连或账号风险。

## 7. 订阅处理：沿用原版流程

原版已经实现“下载订阅 → 解析为统一节点 → 国家/地区选择 → 节点多选 → manifest → sing-box JSON → check/运行”。Windows 直接迁移这条链路，不要求用户改用 sing-box 专用订阅链接，也不另做订阅转换平台。

### 下载和识别

- 沿用 app_proxy_download_subscription 的 User-Agent 顺序：Clash.Meta、Loon、Quantumult X、Surge、Shadowrocket、ClashforWindows、ClashX Pro、Mozilla。
- 原版 curl 请求成功即返回；只有下载失败才换下一个 User-Agent，不是每次解析失败都会继续尝试所有客户端。迁移保留清晰的下载/解析错误边界，增加网络超时和响应大小限制。
- 按原解析器 main 的顺序尝试 Clash YAML、Loon/Surge/Shadowrocket/Quantumult X 风格配置，最后处理普通 URI 或 Base64 节点列表。
- 协议范围沿用原版：AnyTLS、VLESS、VMess、Shadowsocks、Trojan、Hysteria2（含 hy2 别名）。只宣称原解析器已经覆盖并通过迁移测试的字段和变体，不承诺所有 YAML 或所有客户端语法。
- 保留 nodes/unsupported 结构，展示不支持项目和原因，不静默丢失节点；输出中隐藏密码及带 token 的 URL。

### 选择和配置生成

- 沿用节点名称/旗帜推断国家地区的逻辑，提供地区单选和节点多选。
- 选择写入 profile 的节点清单，再由配置生成器创建各节点 outbound、urltest 分组、本地 HTTP inbound 及按入口路由。
- urltest 的测速、出站连接和协议支持由 sing-box 执行；工具只生成配置，不重做代理协议。
- 生成配置后用实际 sing-box 执行 check，再启动或重启本工具实例并探测出口；保留旧可用配置作为失败恢复来源。

### 刷新

- 沿用重新下载、解析、按节点名称匹配并保留已选节点的流程；对重名歧义明确报告，避免错绑。
- 展示保留、新增、消失的已选节点。部分节点消失但仍有有效已选节点时保留剩余选择并应用。
- 若更新会清空已选出口，交互模式重新选择；非交互调用保留旧配置并记录警告。首版有手动刷新，周期调度仍属后续。
- external 模式仍可直接复用其他客户端维护的代理入口，不重复管理其订阅。

### 迁移边界与验收

- 保留原版解析规则、节点结构和配置语义，替换 JXA 的 Foundation/ObjC、osascript、Bash/curl 调度等 macOS 依赖。
- 用原代码派生的无机密样本覆盖各格式、六类协议、地区识别、节点多选和刷新差异；真实订阅样本用于补充兼容性验收，不作为开始迁移的前置条件。
- 适配当前 sing-box 配置合同并执行 check，不把成功解析等同于真实连接成功。
- 不扩展为完整原生 sing-box JSON 导入器或全格式订阅框架，也不把私人订阅发送到第三方转换网站。

源码参考：`app-proxy-installer-core.sh:2386`、`app-proxy-subscription-parser.jxa:859`、`app-proxy-installer-core.sh:3303`、`app-proxy-config-generator.jxa:476`、`app-proxy-cli.sh:469`、`app-proxy-manifest.jxa:483`（均位于 `AppProxyInstaller-scripts/Resources/`）。

## 8. CLI 与交互菜单

下列为能力分组；实现时统一参数规则和帮助文本，所有入口调用同一套功能：

| 命令 | 能力 |
|---|---|
| 不带命令 / setup | 中文菜单、首次配置向导 |
| proxy discover / add / list / edit / remove | 发现、登记和维护外部/托管 profile |
| subscription import / refresh | 导入、预览、节点选择和刷新 |
| core prepare / start / stop / restart / status | 准备程序及管理托管实例 |
| app add / list / edit / bind / remove | 应用登记、参数及代理绑定 |
| launch <appId> | 启动指定应用 |
| shortcut create / remove | 创建、维护稳定 ID 对应的快捷方式 |
| guard enable / disable / status | 启停指定应用的防误触保护，查看后台状态 |
| status / doctor | 状态摘要及按入口/应用诊断 |
| uninstall | 清理本工具资源，默认保留数据 |

快捷方式固定指向已安装的启动器和 appId，不在快捷方式内存放凭据或拼接目标应用参数。重复执行应更新已有登记资源，不重复创建入口。

## 9. 开发里程碑与完成条件

### M1：配置、入口及受控启动基础

- 建立目录、manifest 校验、数据根目录参数、互斥和基础 CLI。
- 完成应用登记及 ProcessStartInfo 启动，提供受控测试应用。
- 验证中文/空格路径、空参数、引号、尾部反斜杠、子进程环境及父进程不变。

### M2：已有代理复用 + sing-box 托管闭环

- 完成 external 登记、候选发现和代理协议诊断。
- 完成程序复用/下载校验、手动 HTTP/SOCKS5 出口、回环入口生成和配置检查。
- 完成托管实例归属、启停、并发启动控制及启动前检查。
- 验证受控应用的 HTTP 和 HTTPS 请求实际经过 sing-box；端口未监听时不创建目标进程。

M2 必须包含 sing-box，不以仅外部端口模式作为首版交付。

### M3：多出口、订阅与配置备份

- 完成多入口路由、应用改绑定、引用检查。
- 迁移原版订阅格式与六类协议的解析、地区分组、节点多选、urltest 配置及刷新选择保持。
- 完成配置检查、一份最近可用备份及失败恢复。
- 验证两个入口分别命中不同受控上游；公网 IP 相同不作为失败判据。

### M4：Guard、Windows 集成、诊断及卸载

- 完成快捷方式、中文菜单、帮助文本和运行依赖检测。
- 完成实际应用运行检测，按能力明确标注适配状态。
- 完成 Guard 当前用户登录任务、精确目标检测、关闭并代理重启、停用入口及防循环；先用受控应用验证。
- 完成安装资源清单、重复安装/操作幂等性和卸载。
- 验证不停止外部代理、不删除用户提供的内核或原应用；默认保留应用/分身数据和可复用备份。

### M5：端到端验收与交付

- 使用实际目标应用验证启动和代理请求，记录应用版本、安装方式及证据。优先 Claude/Codex，但不停止当前用户会话来强行测试。
- 缺少目标应用或可用出口时，完成受控验收，并将对应真实兼容性列为待验证，不报告全面成功。
- 交付中文 README、无机密示例、测试报告、支持矩阵和可运行入口。

## 10. 验收矩阵

| 场景 | 验收证据 |
|---|---|
| 参数与路径 | 受控子进程记录收到的原始参数，与中文、空格、引号等输入一致 |
| 环境隔离 | 子进程收到代理配置，父进程及用户/系统持久环境保持原值 |
| 代理未监听 | 启动失败，受控程序无新启动记录 |
| 普通 TCP/HTTP 服务占用代理端口 | 诊断不能把它报告为代理正常 |
| HTTP 与 HTTPS | 受控代理/上游日志证明请求经过指定入口，HTTPS 不通过解密验收 |
| 实际 sing-box | 记录实际版本，执行 check 和 run，验证真实转发，不以模拟内核替代 |
| 两个出口 | 两入口分别出现在对应上游日志中 |
| external 模式 | 不改外部配置，不启动或终止外部进程 |
| 已运行应用 | 明确显示旧实例复用风险，不误报新配置已生效 |
| 更新失败 | 非法配置不提交，重启失败恢复旧配置；回滚失败有独立故障状态 |
| 订阅变化 | 节点丢失、重名、不支持或空结果有报告，旧可用配置保留 |
| 原版订阅迁移 | 每种原有格式和六类协议有样本；节点字段、unsupported 结果及生成配置符合预期 |
| 节点选择 | 地区分组及多选生效，生成的 urltest 只包含所选节点，并交由真实 sing-box 执行 |
| 并发操作 | 并发启动不产生重复实例，刷新和编辑不互相覆盖 |
| 资源归属 | PID 重用或同名程序不被误杀，未知资源不被删除 |
| Guard 误启动纠正 | 已启用保护的受控主程序直接启动后，被精确关闭并通过预期代理拉起 |
| Guard 正常启动 | 正确代理实例和辅助进程不被误杀，不因保存新绑定就强制重启旧实例 |
| Guard 异常边界 | 代理不可用不拉起、连续失败不无限重启、停用后不干预、同名不同路径程序不受影响 |
| Guard 登录及卸载 | 重复启用只有一份后台任务，禁用/卸载正确移除本工具任务 |
| 路径安全 | 删除验证绝对路径位于管理根目录，拒绝越界路径及不安全重解析点 |
| 重复安装及卸载 | 不重复创建快捷方式，不删除用户程序，默认保留用户数据 |
| 机密 | 日志、命令行与错误消息不出现真实订阅 token/节点密码 |

公共出口探测可配置端点，失败原因独立呈现。测试优先使用本机受控代理与端点；真实公网及真实应用验证单独记录。

## 11. 当前证据与待确定项

- 最新 MSIX 适配：已通过 Windows 包上下文启动器拉起实际 Codex 分身，独立界面和 Codex 数据目录已初始化；真实浏览器 HTTPS 请求确认连接到托管端口并返回所选美国出口。实际 Guard 误启动纠正通过，原实例保持运行；10 项回归测试通过。模型对话和实际升级仍未验收，详见 windows/TEST-RESULTS.md 最后一节。
- 2026-09-18 补充：已实现 Codex 分身目录配置和 Guard 按实例识别，受控普通实例与两个分身共存测试通过。用户指定订阅解析为 85 个 AnyTLS 节点、0 个不支持项，美国出口与指定 DoH 实测成功。真实 Codex 第二实例启动返回 EPERM，后续启动诊断被自动审批拒绝，实际双开和目标应用流量验收仍待完成；见 windows/TEST-RESULTS.md。
- 已静态核对 macOS 的已有代理复用分支、启动器和多入口生成逻辑。
- 已观察到当前机器 PowerShell 7.6.5、Windows build 26200；正式验收重新记录系统和运行时版本。
- 当前目录尚不是 Git 仓库；保留所有原始材料，新增 Windows 实现目录，不覆盖 macOS 脚本。
- 已下载并校验官方 sing-box 1.14.1 Windows x64，自动安装路径也已运行；已执行真实 HTTP/HTTPS 转发、多入口、应用环境、Guard 后台纠正、任务/快捷方式和更新回滚测试。
- 真实应用路径、所需订阅样本和目标出口在相应验收前核实，不妨碍前期独立开发。

实施顺序：M1 → M2 → M3 → M4 → M5。每个里程碑记录已实现、已执行验证和待验证内容，不把静态检查当作运行通过。

## 12. 与原版功能表的逐项对照

核对来源：[原文档“功能介绍”](https://bytedance.larkoffice.com/docx/UPPrdONgno8ijUxbTt2cF5phntg#DFzSd7I3QojoW2xh7SbcfUklnOe)，2026-09-17 读取，文档 revision 1631。

本节记录原表和范围调整；2026-09-18 已完成通用首版及受控验证，具体已执行检查见 windows/TEST-RESULTS.md。原表共 12 项“已支持”功能，以及 2 项“待支持”条目；Guard 已实现，网卡绑定暂不做，设备指纹不属于本次目标。

| 原表功能 | 当前计划覆盖情况 | 差异或需补充内容 |
|---|---|---|
| 为任意 AI .app 生成 Proxy 封装 | 已规划 Windows 对应能力 | 用 EXE 登记、启动器和快捷方式替代 .app；代理参数按应用适配，不承诺任意程序都生效 |
| 自动发现、配置 sing-box | 已规划 Windows 对应能力 | 优先复用已有可用入口，否则复用/下载 Windows 程序并配置托管实例；不用 Homebrew |
| 订阅链接解析 | 首版按原版流程迁移 | 沿用多 User-Agent 下载、Clash/客户端配置/URI/Base64 解析和六类协议，替换 macOS 依赖并补迁移测试 |
| 节点选择 TUI | 首版按原版流程迁移 | 保留国家/地区聚合、地区单选、节点多选及 urltest 出口，测速执行交给 sing-box |
| DNS/DoH 配置 | 已迁移原配置生成逻辑 | 包含预置和自定义 DoH、bootstrap、IPv4-only；实际可用性取决于用户网络，不能把预置服务器列表等同于自动故障切换 |
| 网卡绑定 | 按用户要求暂不做 | 不实现 Wi-Fi/物理网卡选择、主动绑定和回退策略 |
| Claude 特化保护 | 部分覆盖，需适配验证 | 仅有通用启动和真实应用验收；未明确 settings.json 写入、Anthropic 域名规则、限定托管出口及缺少出口时阻止安装的对应策略 |
| Codex 特化配置 | 部分覆盖，需适配验证 | 未明确 .env 和 shell_environment_policy.set 写入、冲突处理、备份和匹配撤销；Windows 当前产品是否消费这些配置须先验证 |
| Guard 防误触 | 首版纳入 | 对明确启用保护且可识别的应用纠正误启动，关闭后由代理入口拉起；不误杀同名或辅助进程，并防止重启循环 |
| 每应用运行时配置 | 已规划等价能力 | 以稳定 ID 对应 JSON 中的独立应用配置和 profile 绑定，支持改端口；不要求复制 config.env 的存储形式 |
| 全链路检查 | 部分覆盖 | 有进程、入口、出口、启动、环境及 Guard 验收；仍需补充 Claude 专属域名请求、专用配置检查和用户可调用的 launcher smoke test |
| 卸载能力 | 通用模式已实现 | 支持全部清理、仅清理托管内核、删除单个登记；专用应用配置尚未写入，因此无对应撤销逻辑。外部代理及用户内核保持不动 |
| 待支持：VPS 自建、HTTP、SOCKS5 手动节点 | HTTP/SOCKS5 已纳入 | “VPS 自建”应区分填写自建节点与自动部署服务器；当前计划不包含远端 VPS 部署 |
| 待支持：独立设备指纹环境 | 不属于本次范围 | 原表底部“待支持”确有此条，不是原版已有功能，也不列为 Windows 必须补齐的缺项 |

### sing-box 版本管理的来源说明

原功能表没有列出版本管理。本地原版 app_proxy_ensure_singbox_environment 的行为是：Homebrew 已安装 sing-box 则返回，否则执行 brew install sing-box。脚本未发现独立的版本选择、固定版本、升级/降级或多版本切换功能。

按最新要求，Windows 也不做版本管理器：优先复用，缺失时安装一份，记录版本并执行 check 即可。保留配置备份和基本失败恢复，不建设多版本目录、自动升级/降级或复杂事务框架。

### 功能对齐时建议的调整

- 原表其余差异主要是专用应用适配及其实际网络验证；DNS/DoH 和分模式卸载已落实，网卡绑定已明确暂不做，订阅及地区分组/多选/urltest 按第 7 节迁移原版。
- Claude/Codex 专用配置需要单独的 Windows 适配验证任务；不能把文件存在或成功写入当作应用实际读取的证据。
- Guard 已纳入首版实际纠正误启动，不能用仅提示替代；它不构成零直连保证。
- 对代理内核的启停、DNS、订阅及配置写入默认只作用于托管实例；external 模式只绑定和诊断代理，但其所绑定的应用仍可启用 Guard。
