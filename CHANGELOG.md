# 决策与改动记录

## 2026-09-18：Guard 快速结束误启动进程

- Guard 纠正请求在原生辅助进程重新核验目标身份后判断：创建后 5 秒内、参数可读、没有代理服务器/PAC 参数且不是辅助进程，直接结束，不发送窗口关闭请求。
- 其他 Guard 纠正正常退出最多等待 500 ms，超时再结束；非 Guard 的停止操作保留原行为。仍只操作已核实的单个进程，不按名称或整棵进程树结束。
- 原生策略回归覆盖刚启动、较老进程、已有代理、辅助进程、参数不可读及非 Guard 停止；真实集成增加 Guard 快速路径拒绝创建时间不匹配的验证。

## 2026-09-18：纠正时按需等待并复用检查

- 去掉关闭后的固定 300 ms 等待，立即检查剩余进程；仅当剩余项全是辅助进程时，每隔 100 ms 重试，最多 3 次。发现新的主进程或辅助进程持续存活时停止本次纠正，不额外结束它们。
- Guard 在同一配置锁内把已解析的应用对象和最后一次进程快照传给启动流程，省去重复包解析及进程扫描；普通手动启动仍执行完整检查。进程结束前的身份校验、代理验证及启动回执校验保留。
- 修改前现场 Claude 两次纠正为 5429 / 4562 ms，固定等待及重复检查合计约 750 ms；剩余主要耗时是正常关闭和 MSIX 启动，本次未修改正常关闭宽限时间。

## 2026-09-18：刷新 Guard 与纠正耗时诊断

- 纠正仍闪窗的现场 Guard 启动于 21:01，早于隐藏 MSIX 辅助进程修复。同步代码后刷新常驻 Guard，并确认 ETW 重新就绪，Claude 现有 PID 保持不变。
- 关闭进程时仅在成功发送窗口关闭请求后等待 1.5 秒；没有主窗口则直接进入已有的终止流程，保留 PID、创建时间、路径及用户归属校验。隔离无窗口进程对照：旧版 2027 ms，新版 502 ms。
- 增加 `guard-timing`，记录发现时进程年龄、停止、清理、包解析、进程检查、代理就绪、启动、回执和纠正总耗时。日志不写入代理凭据或应用参数。
- 只读现场采样：包解析 146–279 ms，进程扫描 263–325 ms，内核身份检查 201–241 ms，代理 HTTPS 探测 189–205 ms；不能将这些独立采样等同于完整 Claude 纠正时延。

## 2026-09-18：隐藏 MSIX 启动辅助进程

- MSIX 包内辅助入口改用 `wscript.exe → hidden.vbs → node.exe`，避免直接激活控制台版 Node 时出现命令行窗口；目标应用仍按原方式正常显示。
- 类型检查和 6 项 MSIX 回归通过；分别在本机 Codex、Claude 包上下文验证隐藏辅助链路能返回过期请求回执，没有启动或结束用户应用。没有进行完整目标应用启动的肉眼闪窗验收。

## 2026-09-18：快捷方式使用应用名称

- 分身默认名称也使用应用名称，不额外加“分身”文字；快捷方式如 `Claude - 12345678.lnk`，旧默认名称中的“分身”后缀在生成快捷方式时去掉。
- 原版快捷方式使用应用名称（如 `Claude.lnk`），仅分身附加 8 位实例 ID；保留原应用图标和代理启动入口。
- 重新创建已登记快捷方式时更新旧名称并同步登记路径；同名未登记文件不覆盖。应用名称过滤 Windows 文件名非法字符及保留设备名。
- 类型检查通过；隔离目录实测原版命名、旧名称迁移、分身后缀、同名文件保护及删除。已同步用户当前安装目录，并将桌面 `App Proxy - 3541ab24.lnk` 更新为 `Claude.lnk`。

## 2026-09-18：Codex / Claude 内置添加入口

- 添加应用先选 Codex / Claude / 其他应用；内置入口只需选择原版或空白分身，再完成代理配置，自动填充名称、EXE 和 Chromium 适配，不询问附加参数。
- 通过当前用户的包身份及主 AppId 读取实际 EXE，不固定 WindowsApps 路径或版本。实机 Codex 主入口为 `app/ChatGPT.exe`，Claude 主入口为 `app/Claude.exe`；排除 Claude 的 SSH 辅助入口。
- 默认 Guard 同时识别 Codex 主包身份，修复仅凭 Codex.exe 文件名漏掉实际桌面主程序的问题；普通 ChatGPT 不因此默认启用 Guard。
- CLI 支持 `app add codex|claude <profileId|direct> [--clone]`，保留 JSON 高级入口。

## 2026-09-18：先准备代理，再添加应用

- 添加应用先选择/发现代理，无可用入口时内联创建订阅或手动上游；名称、空闲监听端口有默认值，成功后直接绑定，不要求用户退出重来或重新选择新代理。
- 代理联网验证成功才继续填写和登记应用；失败保留代理配置供修改，取消返回菜单，不隐式直连。
- 独立新增代理后也可直接继续添加应用。复用服务只验证，自建实例按所选配置启动和验证。
- 新增菜单流程回归，覆盖订阅、手动、已有服务、单一入口默认选择、失败、取消、显式直连和创建后继续；真实内核集成测试覆盖新的准备入口。

## 2026-09-18：Codex / Claude 默认保护

- 菜单和 CLI 统一通过 `Service.addApp` 添加。确认使用 Chromium 适配的 Codex.exe / Claude.exe，以及两类分身，绑定代理后默认调用 Guard 启用流程，首次仍走 UAC。
- 不凭显示名识别，不默认保护环境变量适配的 CLI 或直连应用；支持 JSON `guard:false` 和菜单手动停用。不改已有登记。
- UAC 取消时保留新登记但不误报已保护，明确提示重试入口。新增三项服务流程测试，连同四项 Guard 事件回归通过，类型检查通过。

## 2026-09-18：直接 ETW 进程通知

- 以本地提交 `140ac9f` / 标签 `baseline-wmi` 保存变更前基线。
- 保留既有 UAC 任务、受保护脚本、通知管道和普通权限 Guard，仅替换通知源。直接订阅 Kernel-Process ProcessStart，每约 100ms 主动刷新，TDH 解析事件字段，不新增外部库或驱动。
- 退出、升级、卸载清理本工具会话；强制结束后下次启动恢复，错误或丢失事件回到扫描兜底。
- 两轮 8 次通知延迟分别 12–117ms、17–132ms；32 个短进程全部收到；10 秒监听进程 CPU 约 47ms。复现脚本及脱敏摘要入库，详见 `windows/TEST-RESULTS.md`。
- 用户现有安装未自动更新；新版通过 Guard 菜单“修复监听授权”更新并刷新 Guard。

## 2026-09-18：ETW 尝试前基线

- Windows 使用 Node.js 24 / TypeScript、系统 PowerShell 5.1 与必要的 C# 原生接口，中文终端菜单。
- sing-box 优先复用已有服务或程序，缺失时协助安装；仅管理工具自己创建的配置实例，不恢复通用 external 类型。
- 支持原安装应用和独立 profile 分身，MSIX 根据实际安装信息解析，不依赖固定版本路径。不改 Claude settings。
- Guard 普通权限常驻，登录任务拉起；进程监听统一采用经 UAC 授权的受保护管理员辅助任务，命名管道只传通知，应用不继承管理员权限。
- 基线事件源为 Win32_ProcessStartTrace。8 个实测样本的创建到 WMI 通知延迟为 1301–1996 ms，管道末段约 0–3 ms。完整测试 38 项通过，详见验证记录。
- 快捷方式仍使用 App Proxy - ID 名称，原快捷方式替换尚未实现。Guard 是误启动后的纠正，不保证首个网络请求前接管。
- 下一步在现有权限及管道边界内验证直接 ETW 和主动缓冲刷新，对比通知延迟、事件完整性及资源占用。
