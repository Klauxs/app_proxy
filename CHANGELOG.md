# 决策与改动记录

## 2026-09-18：ETW 尝试前基线

- Windows 使用 Node.js 24 / TypeScript、系统 PowerShell 5.1 与必要的 C# 原生接口，中文终端菜单。
- sing-box 优先复用已有服务或程序，缺失时协助安装；仅管理工具自己创建的配置实例，不恢复通用 external 类型。
- 支持原安装应用和独立 profile 分身，MSIX 根据实际安装信息解析，不依赖固定版本路径。不改 Claude settings。
- Guard 普通权限常驻，登录任务拉起；进程监听统一采用经 UAC 授权的受保护管理员辅助任务，命名管道只传通知，应用不继承管理员权限。
- 基线事件源为 Win32_ProcessStartTrace。8 个实测样本的创建到 WMI 通知延迟为 1301–1996 ms，管道末段约 0–3 ms。完整测试 38 项通过，详见验证记录。
- 快捷方式仍使用 App Proxy - ID 名称，原快捷方式替换尚未实现。Guard 是误启动后的纠正，不保证首个网络请求前接管。
- 下一步在现有权限及管道边界内验证直接 ETW 和主动缓冲刷新，对比通知延迟、事件完整性及资源占用。
