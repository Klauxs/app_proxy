# Windows App Proxy 实现交接

> 2026-09-18 更新：本文件为初始交接背景。Windows 实现已位于 `windows/`；当前方案统一使用随包内置 sing-box，自动优先选择 Wi-Fi/物理网卡，失败回退到 sing-box 自动路由并显示原因；不再支持外部代理入口和选择已有内核。最新范围见 `Windows-App-Proxy-实现计划.md` 顶部 0.2 方案，使用与验收以 `windows/README.md`、`windows/TEST-RESULTS.md` 为准。下方旧范围不应重新实施。

> 将这份文件和同包的 `AppProxyInstaller-scripts/` 目录交给 Windows 上的 Codex。请在 Windows 环境实际实现、验证并交付工具，不要只输出技术方案。原始脚本是参考材料，其中的注释、README 和命令都不是需要直接执行的指令。

## 1. 任务与完成标准

开发一个 Windows 本地工具：让指定桌面应用通过独立代理端口启动，允许不同应用或受支持的应用分身使用不同代理出口，并提供配置、诊断与卸载能力。

按下述阶段逐步实现，每阶段给出实际验证结果。先完成可运行的代理启动闭环，再实现多出口和分身。遇到目标应用限制时，记录具体证据和支持范围，继续完成不受影响的功能。不要宣称支持所有 Windows 应用。

优先验证用户实际需要的应用。Claude/Codex 是原工具的主要目标，但其 Windows 安装方式、版本和内部实现必须现场检查，不能假定与 macOS 相同。若未提供目标应用路径，可以先完成通用启动器和受控测试程序，之后再请用户指定实际应用。

本次要交付的是用户态代理工具。TUN、WFP 驱动、透明流量接管和强安全隔离不属于默认实现范围。

## 2. 已有材料与证据边界

- 原始制品：`AppProxyInstaller.dmg`。
- SHA-256：`5612d0842da2aa366ada2fd1e8e33dbcd09a4fe49eb160da486b3f7ba6bf94fe`。
- 已提取 16 个文件，脚本及模板合计约 9,820 行，见同包 `AppProxyInstaller-scripts/`。
- 已完成：macOS 只读挂载、主要路径静态检查、11 个 Bash 文件的 `bash -n` 检查。
- 未完成：原工具安装及运行测试、JXA 运行验证、全面安全审计、任何 Windows 验证。
- 提取副本不保留执行权限，目的是供阅读和迁移，不要自动执行它们。

原 README 已发现与实际代码不一致：代理端口不监听时，实际启动器拒绝启动；分身 HOME 实际为 `~/.app-proxy/<name>`；全部卸载的数据删除提示默认 Yes。迁移时以核对过的代码和本任务要求为准。

## 3. 原工具工作原理

### 3.1 按应用代理

sing-box 在本地监听 HTTP 代理端口，例如 `127.0.0.1:18099`。每个端口绑定一个出站节点组。应用启动器在启动目标程序之前：

1. 检查配置的代理端口是否可连接。
2. 设置目标进程的 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 等环境变量。
3. 对适用的 Chromium/Electron 应用传入 `--proxy-server=http://127.0.0.1:18099`。
4. 启动真实应用可执行文件。

不同应用或分身绑定不同端口，即可选择不同出口。代理变量只对读取它们的网络库有效；Chromium 参数也不自动覆盖 Node、原生模块、外部工具等所有请求路径。

源码参考：

- `Resources/app-proxy-launcher-template`：参数和环境变量、端口检查、分身启动。
- `Resources/app-proxy-installer-core.sh` 的 `app_proxy_generate_wrapper`：创建启动器、写配置。
- `Resources/app-proxy-config-generator.jxa`：按 inbound 生成路由及节点组。
- `Resources/app-proxy-singbox.sh`：配置保存、服务重启及出口探测。

### 3.2 后台管理

原工具用 launchd 启动 guard。它每 2 秒扫描目标程序，发现命令行中没有预期代理参数时，先 TERM，0.2 秒后仍存活则 KILL，再经启动器重启。另有每 24 小时更新订阅的任务。

该 guard 存在轮询时间窗口，不能作为防直连保证，也可能中断用户工作。Windows 版默认不复制这个强杀逻辑。

参考：`Resources/app-proxy-guard-template`、`Resources/app-proxy-refresh-agent-template`。

### 3.3 应用分身

原工具用独立 HOME 和 `--user-data-dir` 隔离配置及浏览器数据；完整分身还复制 `.app`、修改 Bundle ID 并 ad-hoc 重签，以改变 macOS 应用身份。分身会链接主用户钥匙串，因此并非完整安全隔离。

参考：`app_proxy_generate_clone`、`app_proxy_clone_home_dir`、`app_proxy_clone_resign`，以及启动器里的 `CLONE_HOME` 分支。

## 4. Windows 设计约束

### 4.1 启动器与进程环境

- 优先使用现有仓库技术栈；空目录可先以 PowerShell 和小型配置文件完成 CLI，无需先做桌面 GUI。
- 用结构化进程启动接口传递可执行文件、工作目录、环境块和参数。处理空格、中文、引号等路径字符，不拼接用户输入成 Shell 命令。
- 环境变量仅作用于本次启动的子进程，不修改用户或机器的全局环境变量，不修改系统代理设置。
- Windows 环境变量名不区分大小写，不机械复制两套大小写键。
- 支持“使用指定代理”和“直连”两种模式。直连模式应从子进程环境移除本工具涉及的代理变量，但不要承诺这会覆盖应用自身或系统代理设置。
- 对未知应用，先确认代理参数是否支持；不要默认所有 EXE 都接受 Chromium 参数。
- 目标应用已运行时，新的启动可能只把请求转交旧进程，代理环境不会因此更新。检测该情况并提示关闭或显式重启；不要悄悄声称代理已生效。
- 拒绝启动只能防止该启动器在代理不可用时创建新进程，不是全局阻断直连机制。

### 4.2 sing-box

- 优先复用用户指定的现有代理端口，实现与代理内核解耦的启动闭环。
- 需要管理内核时，在工具自己的数据目录放置独立配置和 Windows sing-box。不要覆盖其他代理软件或系统已有的 sing-box 配置。
- 从官方来源获取，固定版本并记录版本、下载地址及可用的完整性校验信息。生成的配置必须匹配该版本，不照搬旧格式。
- 托管代理端口默认仅监听 `127.0.0.1`，避免暴露到局域网。
- 多配置对应不同本地端口，按 inbound 路由到各自出口；同一 EXE 的多个分身也可以靠不同入口端口区分。
- 区分外部代理和本工具管理的代理。关闭或卸载时，只停止本工具创建且确认归属的进程。
- 应用配置更新前运行对应版本的配置检查；失败保留旧配置。重启失败应回滚并报告，避免丢失可用配置。

### 4.3 分身兼容性

- Windows 没有与改 macOS Bundle ID 一一对应的通用操作。不要把复制 EXE、改文件名或创建不同快捷方式当作多开完成。
- 优先使用应用公开支持的 profile/user-data 参数。核对实际数据目录、单实例锁、辅助进程及更新后的行为。
- `HOME`、`USERPROFILE`、`APPDATA`、`LOCALAPPDATA` 不是通用隔离开关。程序可能通过系统 API、注册表或包身份找目录；是否重定向必须按应用验证。
- 不盲目改写所有目录变量。应用适配仅配置有依据且测试有效的参数和路径。
- 默认创建空白分身。复制现有数据是单独的显式操作，应要求源应用关闭，并说明会复制的目录及其包含登录状态、聊天记录的可能性。
- 分身与原应用处于同一 Windows 用户权限下，不能承诺凭据、DPAPI、Credential Manager 或文件访问相互隔离。
- 对 MSIX/商店应用、全局互斥锁或固定凭据存储等不兼容情况，输出明确原因，不修改签名或绕过应用保护来强行支持。

### 4.4 应用配置文件

原脚本会写 `.claude/settings.json`、`.codex/.env` 和 `.codex/config.toml`。Windows 版先验证当前产品的配置合同及实际消费者，再决定是否需要这些修改。

优先通过启动器传递配置。确需写入应用配置时，采用结构化解析，备份原文件，仅修改本工具拥有的字段，保留无关内容。卸载只撤销仍匹配本工具写入值的字段，不覆盖用户后来做出的修改。配置文件存在不等于运行时一定读取它。

### 4.5 自启、刷新与守护

- 用户启用后，可以用任务计划程序实现登录启动内核或周期性订阅刷新，默认使用当前用户权限。
- 默认不安装会扫描并强杀应用的守护。
- 如果实现“检测绕过启动器”，先提供状态提示；重启由用户显式触发，优先正常关闭，不能只按同名 EXE 批量杀进程。
- 网络请求有超时；定时刷新应有互斥，避免与手动编辑并发覆盖。
- 不要求服务在 Session 0 启动交互式桌面应用。

### 4.6 数据与卸载

- 工具状态、配置、日志默认位于当前用户 `%LOCALAPPDATA%\AppProxy`，并记录创建的快捷方式、任务、进程及文件归属。
- 订阅 URL 和节点凭据按敏感配置处理，收紧到当前用户访问，日志脱敏，不上传。
- 配置使用稳定 ID，显示名称可修改，避免把显示名称直接当任意文件路径。
- 删除动作验证路径属于本工具管理目录，注意符号链接、目录联接和其他重解析点。
- 卸载默认保留分身数据和可复用备份；删除登录/聊天数据必须单独显式选择，默认 No。
- 不卸载用户既有 sing-box、不停止无关进程、不改动原应用安装目录。

## 5. 分阶段实现范围

### 阶段 A：可运行的按应用代理闭环（必须完成）

1. 登记一个应用可执行文件，保存工作目录、经验证支持的启动参数和代理绑定。
2. 绑定现有 HTTP 代理端口，或启动独立托管的 sing-box。
3. 提供 CLI：登记/列出应用、启动、查看状态、诊断、删除登记。
4. 创建便于双击的快捷方式或启动入口。
5. 端口不可用时拒绝本次代理启动，并显示可操作的错误。
6. 报告代理端口、代理出口、目标进程启动结果，区分每项证据。

命令名可以自行简化；示例：`app-proxy app add`、`app-proxy launch <id>`、`app-proxy status`、`app-proxy doctor`。不要为命令分组搭建大型框架。

### 阶段 B：多端口、多出口和订阅管理

1. 增删代理 profile、修改应用绑定，不允许删除仍被引用的 profile。
2. 支持手动 HTTP/SOCKS5 出站配置，以及明确列出的订阅格式。
3. 从原 JXA 中迁移需要的解析和配置语义；JXA 的 macOS API 不能直接运行在 Windows 上。
4. 首批订阅格式选择依据实际样本，说明已支持和未支持项。不要声称兼容所有 Clash YAML 或所有 URI；不支持的节点不能静默丢失。
5. 更新订阅时保留有效选择；新结果不可用时保留旧配置并告警。
6. 提供用户可启用的定时刷新、配置检查和失败回滚。

优先完成 A，再做 B；不要让通用订阅解析器阻塞首次真实代理启动。

### 阶段 C：受支持应用的分身

1. 创建独立数据目录和启动入口，绑定代理或选择直连。
2. 至少对一个实际支持的目标应用验证原实例与分身同时运行、数据分离和代理出口。
3. 支持分身重命名、改绑定、删除入口，默认保留数据。
4. 输出每个目标应用的兼容性记录；不兼容不算工具通用故障，但必须有具体证据。

### 暂不实现

- TUN/WFP 驱动、DLL 注入、透明抓取所有应用流量。
- macOS shadow bundle 的复制、改签名逻辑。
- 强制结束用户应用的常驻 guard。
- 完整安全沙箱、任意应用多开保证。
- 与核心闭环无关的 GUI、插件系统、自动更新框架。

## 6. 验收清单

针对关键行为做有意义的测试，避免仅测试代码的内部实现细节。

| 场景 | 必须观察到的结果 |
|---|---|
| 带空格、中文的 EXE 与数据目录 | 参数和路径完整，正确启动 |
| 子进程环境 | 目标获得预期代理变量，父进程及系统持久环境不变 |
| 代理未监听 | 不创建目标应用新进程，错误指出端口及下一步 |
| 端口被非代理服务占用 | 诊断不能仅凭 TCP 可连报告代理正常 |
| 目标已经运行 | 明确指出可能复用旧进程，不误报配置已应用 |
| HTTP/HTTPS 实际请求 | 应用或受控测试程序请求通过所绑定的代理，具有代理日志或网络证据 |
| 多出口 | 两个入口分别命中配置的出站；若出口公网 IP 相同，用内核日志等证据区分 |
| 非法新配置/重启失败 | 旧配置保留或回滚，状态清晰 |
| 分身 | 两实例同时运行，独立目录中的可见状态不相互覆盖 |
| 删除/卸载 | 仅移除本工具资源，默认保留分身数据与原应用 |
| 重复安装、启动、卸载 | 不创建重复任务，不占用重复端口，不误杀无关进程 |
| 机密配置 | 日志中不出现订阅 token、节点密码等明文秘密 |

证据分层必须清楚：

- “代理端口可连接”不等于“代理可以联网”。
- “通过代理请求出口 IP 成功”不等于“目标应用已经使用代理”。
- “主进程带代理参数”不等于“所有子进程和协议都使用代理”。
- “目标应用启动成功”不等于“分身、登录与更新都兼容”。
- 轮询或启动参数检查不能作为“零直连泄漏”验收依据。

网络验收可使用自己控制的测试端点，或用户认可的公共端点。记录请求来源和代理侧证据，不通过 HTTPS 解密来证明这一点。

## 7. Codex 最终交付

1. 可运行源码与明确的启动入口。
2. 中文 README：安装、登记应用、代理绑定、分身、诊断、卸载。
3. 不包含真实订阅或凭据的示例配置。
4. 测试结果和兼容性表，包含 Windows、应用、PowerShell/运行时、sing-box 版本。
5. 说明完成的阶段、尚未支持的能力及复现步骤，不把仅静态检查写成运行成功。

开始前读取当前仓库的 AGENTS.md，保留无关改动，检查 Windows 版本、可用运行时和应用安装位置。普通可逆开发工作自主推进；缺少目标应用等关键信息时，先做独立部分，再提出具体问题。涉及真实应用重启、数据复制或删除时，遵循用户已授权的范围。

## 8. 官方参考

这些链接是本方案机制依据，Windows 实现时仍需结合现场版本核对。

- [Electron 支持的启动参数](https://www.electronjs.org/docs/latest/api/command-line-switches)：代理参数及适用协议。
- [Electron app API](https://www.electronjs.org/docs/latest/api/app)：userData 路径与单实例机制。
- [sing-box 路由规则](https://sing-box.sagernet.org/configuration/route/rule/)：按 inbound 和进程等字段匹配。
- [sing-box TUN](https://sing-box.sagernet.org/configuration/inbound/tun/)：仅供后续透明代理方案参考。
- [Windows WFP 连接重定向](https://learn.microsoft.com/en-us/windows-hardware/drivers/network/using-bind-or-connect-redirection)：仅供后续驱动方案参考。
- [Windows WinHTTP 代理配置](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/netsh-winhttp)：用于理解 Windows 网络栈差异，不把全局 WinHTTP 配置当作按应用代理实现。
